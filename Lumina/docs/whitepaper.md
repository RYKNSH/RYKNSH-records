# Lumina Whitepaper: The World-Class AI Creative Production

> **このドキュメントは Lumina 開発の北極星である。**
> - 開発セッション開始時に必ず参照すること
> - 方向性に迷ったときに立ち返ること
> - 全ての設計判断はこのドキュメントに基づくこと

---

## 1. Luminaとは何か

**「納品物の絶対的クオリティが世界最高峰レベルを更新し続ける、全自動AIクリエイティブプロダクション」**

Luminaは画像生成ツールではない。動画編集ソフトでもない。テンプレート配信サービスでもない。
Luminaは **「市場の美学を先読みし → 最新AIモデルを自動選別し → マルチモデル合成で単一モデルの限界を突破し → AI審美眼で美的品質を保証し → 品質基準未達なら自動でリトライし → 常に世界最新の技術とスタイルを取り込み続ける」** という
**「世界最高峰のクリエイティブを、人間のデザイナー0名で、無限にスケールさせる」自律型AIプロダクション**だ。

**光（Lumina）は暗闇からアセットを具現化する。** しかし、ただ光を当てるだけではない。**世界で最も美しい光**を当てる。

### センターピン

> **納品物の絶対的クオリティが世界最高峰レベルを更新し続けること。**

これがLuminaの存在意義であり、全ての設計判断の最上位原則。速度もコストも、この一点にのみ仕える。

### 差別化ポイント

1. **Quality Fortress（品質要塞）**: Multi-Critic審美眼 + Score Cascade + Auto-Retry + Predictive QCの多層防衛。「まぁいいか」が通るパスが物理的に存在しない
2. **Model Registry + SOTA Watchdog**: 新モデルの自動検出 → 自動ベンチマーク → 自動切り替え。コード変更なしで常に最新最強のモデルを使う
3. **Multi-Model Synthesis + Enhancer Pipeline**: 単一AIモデルの天井を、複数モデルの合成とポストプロセッシングで突破する
4. **6社の内販実績**: Label 01のMV、Cyrusの広告素材、Irisのプレスキット——全てLuminaが作る。自社で実証済みの最高品質が外販の信頼基盤

---

## 2. RYKNSH recordsにおける役割

### ホールディングス内でのポジション

```
RYKNSH records (Holdings / Mothership)
├── ① Velie   — QA/検証（全社の品質基盤）
├── ② Lumina  — AI制作プロダクション ← ここ
├── ③ Cyrus   — グロース・マーケティング・自動営業
├── ④ Iris    — 広報PR・ブランド防衛
├── ⑤ Ada     — AI R&D・全エージェントの中枢基盤
├── ⑥ Noah    — ファン・コミュニティ基盤
└── ⑦ Label 01 — 音楽レーベル・フラッグシップIP
```

### 内販フロー

| 供給先 | モデル | 供給アセット | 課金 |
|--------|-------|-------------|------|
| **⑦ Label 01** | B2C | アートワーク・MV・ジャケット・VI | 案件ベース |
| **③ Cyrus** | B2B | SNS広告クリエイティブ・LP素材・バナー・動画広告 | 案件+月額 |
| **④ Iris** | B2B | プレスキット・メディアアセット・ブランドガイドライン素材 | リテイナー |
| **⑥ Noah** | B2C | コミュニティ内ビジュアル・限定コンテンツ素材 | 月額 |
| **本社** | — | コーポレートアイデンティティ・全社ブランドガイドライン | 都度 |

### 教育事業（内包）

ディベートRound 8で「教育はクリエイティブ制作の延長」として Lumina に吸収が確定。AIクリエイティブツールのチュートリアル、AIとの協業ワークショップ、L0アーティスト育成コンテンツ。

---

## 3. 世界最高峰を阻むボトルネック6層と対策

| # | ボトルネック | 意味 | 対策 |
|---|------------|------|------|
| 1 | **Model Freshness** | 使用AIモデルが最新か | **Model Registry** + SOTA Watchdog（新モデル自動検出・ベンチマーク・切り替え） |
| 2 | **Taste Engine** | 「美しい」「ダサい」の判定力 | **Multi-Critic Ensemble** + **Autonomous Taste Calibration**（Market Signal → Cross-Validation → Auto-Update → Self-Validation） |
| 3 | **Context Depth** | クライアントの世界観の深い理解 | **Reference Set** (pgvector) + ブランド深層プロファイリング |
| 4 | **Iteration Velocity** | フィードバックサイクルの速度 | 並列生成 + Auto-Retry Loop |
| 5 | **Ceiling Breaker** | 単一モデルの限界の突破 | **Multi-Model Synthesis** + Enhancer Pipeline |
| 6 | **Quality Culture** | 「妥協」が通るパスの排除 | **Quality Fortress**（Score Cascade + Predictive QC） |

---

## 4. Entity Model（B2B/B2C/C2C統一）

```
Entity (抽象)
├── Company (B2B)
│   ├── 企業のマーケ部門、スタートアップ
│   └── ニーズ: ブランドロゴ・LP・広告クリエイティブの受託
│
├── Consumer (B2C)
│   ├── 個人クリエイター、インディーアーティスト
│   └── ニーズ: MV自動生成・SNS素材・サムネイル
│
└── Creator (C2C)
    ├── プロのビジュアルアーティスト
    └── ニーズ: AIスタイルパック（美学のパッケージ）の作成・売買
```

---

## 5. 5-Layer Architecture

```
┌──────────────────────────────────────────────────────────┐
│  🔵 INTELLIGENCE — 何が最高かを知る（週次）                  │
│  sota_watchdog → style_frontier_tracker → model_benchmarker│
├──────────────────────────────────────────────────────────┤
│  🟢 CREATION — 最高を作る（毎リクエスト）                     │
│  brief_interpreter → model_selector →                      │
│  [generator | multi_model_compositor] → enhancer_pipeline   │
├──────────────────────────────────────────────────────────┤
│  🟡 QUALITY FORTRESS — 最高を保証する（毎生成物）             │
│  taste_engine (multi_critic_ensemble) →                     │
│  quality_score_cascade → [deliver | retry | escalate]       │
├──────────────────────────────────────────────────────────┤
│  🟠 DELIVERY — 最高を届ける（毎納品）                        │
│  format_optimizer → asset_packager → brand_consistency_check│
├──────────────────────────────────────────────────────────┤
│  🔴 EVOLUTION — 最高を更新し続ける（日次/週次）               │
│  performance_analyst → taste_calibrator →                   │
│  predictive_qc → sota_updater → (feedback → Intelligence)   │
└──────────────────────────────────────────────────────────┘
```

### 各レイヤーの動作頻度

| レイヤー | 頻度 | 機能 |
|---------|------|------|
| 🔵 Intelligence | 週次 | 最新AIモデル監視・美学トレンド追跡・モデルベンチマーク |
| 🟢 Creation | 毎リクエスト | ブリーフ解析→最適モデル選択→生成→エンハンス |
| 🟡 Quality Fortress | 毎生成物 | 多角的審美評価→品質基準比較→自動リトライ/AI Escalation Chain |
| 🟠 Delivery | 毎納品 | フォーマット最適化→パッケージ化→ブランド一貫性最終検査 |
| 🔴 Evolution | 日次/週次 | パフォーマンス分析→Taste校正→予測的品質管理→SOTA自動更新 |

---

## 6. 全20ノード

### 🔵 Intelligence Layer（3ノード）

| ノード | 機能 |
|-------|------|
| `sota_watchdog` | arXiv/HuggingFace/主要AIサービスの新モデル自動スキャン。有望モデル検出→自動ベンチマーク→切り替え推奨通知 |
| `style_frontier_tracker` | Behance/Dribbble/Pinterest/ファッション/映画のビジュアルトレンド分析。「来月流行るスタイル」の予測。参照セットへの自動追加 |
| `model_benchmarker` | Model Registryの全モデルを定期的に参照セットで評価。capability_scoreの自動更新。劣化モデルの自動降格 |

### 🟢 Creation Layer（4ノード）

| ノード | 機能 |
|-------|------|
| `brief_interpreter` | クライアントブリーフの深層解析。曖昧な指示を構造化された生成パラメータに変換。参照セット（pgvector）から類似成功作品を検索してコンテキスト注入 |
| `model_selector` | タスク性質（リアリスティック/アート/アニメ）× 品質ティア × コスト制約から最適モデルを動的選択。Model Registryのcapability_scoreベース |
| `generator` / `multi_model_compositor` | 単一モデル生成 or マルチモデル合成。Premiumティア以上では複数モデルの出力をレイヤー合成して天井を突破 |
| `enhancer_pipeline` | 超解像（Upscaler）→ カラーグレーディング → ディテール注入 → 最終研磨。どのモデルを使っても最終出力が最高品質になることを担保 |

### 🟡 Quality Fortress（3ノード）

| ノード | 機能 |
|-------|------|
| `taste_engine` | **Multi-Critic Ensemble**: 構図評価Critic / 色彩評価Critic / ブランド適合Critic / 感情インパクトCriticを並列実行→重み付き統合スコア |
| `quality_score_cascade` | Taste Engineスコアをテナント要求水準と比較。未達→Auto-Retry（品質ティアに応じて最大3-10回）。リトライ上限→AI Escalation Chain |
| `ai_escalation_chain` | **4段階のAI自律エスカレーション**: Strategy Shift（モデル/プロンプト変更）→ Multi-Model Escalation（ティア自動昇格）→ Creative Decomposition（要素分解→個別生成→合成）→ Graceful Degradation（テナントに代替案を自動提示） |

### 🟠 Delivery Layer（3ノード）

| ノード | 機能 |
|-------|------|
| `format_optimizer` | 出力フォーマットの最適化。Web用/印刷用/SNS用に解像度・色空間・圧縮率を自動調整 |
| `asset_packager` | 納品物のパッケージ化。ファイル命名規則・メタデータ付与・バージョン管理・S3/Storageアップロード |
| `brand_consistency_check` | 最終関門。テナントのブランドガイドライン（カラーコード・フォント・ロゴ使用規則）との適合度を最終検証 |

### 🔴 Evolution Layer（4ノード）

| ノード | 機能 |
|-------|------|
| `performance_analyst` | 全プロジェクトのKPIダッシュボード。品質スコアトレンド・リトライ率・納品速度・コスト効率の追跡 |
| `taste_calibrator` | **Autonomous Taste Calibration**: Market Signal Collector（Award/Buzz/Competition/Style）→ Multi-Signal Cross-Validation（一過性バズ排除）→ Taste Genome Auto-Update → Self-Validation Gate（A/Bテスト+ロールバック）。**ローンチ後の人間関与ゼロ** |
| `predictive_qc` | 品質低下の予防的検知。品質スコアの下降トレンドを検出→原因分析（モデル劣化？参照セット陳腐化？Criticバイアス？）→自動是正 |
| `sota_updater` | SOTA Watchdog/Style Frontier Trackerの結果をModel Registry・参照セットに自動反映。L3ではモデル切り替えも自動実行 |

---

## 7. Model Registry（核心インフラ）

全AIモデルをSupabase上のレジストリで一元管理。**新モデル追加はコード変更なしでレジストリに1行追加するだけ。**

```sql
CREATE TABLE lumina_model_registry (
  id uuid primary key,
  model_name text not null,
  model_type text CHECK (model_type IN ('image', 'video', 'audio', 'composite')),
  provider text,
  api_endpoint text,
  capability_score numeric,
  cost_per_call numeric,
  strengths jsonb,  -- {"composition": 0.9, "text_rendering": 0.3, "photorealism": 0.8}
  weaknesses jsonb,
  is_active boolean default true,
  last_benchmarked_at timestamptz,
  created_at timestamptz default now()
);
```

`model_selector`ノードはこのレジストリを毎回参照し、タスクに最適なモデルを動的に選択する。

---

## 8. Taste Engine 詳細

### Multi-Critic Ensemble

```
生成物 → Critic A (構図・レイアウト)     ← 同時並列
       → Critic B (色彩・トーン)
       → Critic C (ブランド適合度)
       → Critic D (感情インパクト)
       → Critic E (IP世界観適合度)    ← Masterpieceティアのみ
       → Critic F (ファン期待値)      ← Masterpieceティアのみ
                    ↓
          Taste Aggregator
          (重み付き統合スコア: 0-100)
```

### Reference Set（参照基盤）

テナント/IP/プロジェクトごとに「これが最高」という参照セットをpgvectorに格納。生成物の品質は**絶対評価ではなくこの参照群との相対評価**で決まる。

### Autonomous Taste Calibration（自律的審美校正）

> **ローンチ後の人間の関与はゼロ。** Taste Engineは自律的に進化する。

| Phase | 人間の関与 | 内容 |
|-------|----------|------|
| **Pre-Launch**（2週間） | ✅ CEOのみ | Seed Genome Strategy: Public Curated Datasets（Behance/Dribbble/Awwwards受賞作品）+ Genre Lattice（16ジャンル×50作品）でTaste Genomeの初期値を構築 |
| **ローンチ後** | ❌ 完全不要 | Autonomous Taste Calibration Loopが週次で自動実行 |

**品質自律ループ（週次サイクル）:**
1. **Market Signal Collector**: Award受賞作品 / 10K+エンゲージメント作品 / 競合ショーケース / スタイルトレンド変遷を自動収集
2. **Multi-Signal Cross-Validation**: Award × Buzz × 競合採用 × 持続期間4w+の交差点のみを「Validated Trend」に認定。一過性バズを自動排除
3. **Taste Genome Auto-Update**: Validated TrendをReference Setに追加（古い参照はdecay）→ Critic重みを自動調整
4. **Self-Validation Gate**: 旧Genome vs 新Genomeで同一リクエストをA/Bテスト → 新Genomeが上回る場合のみ更新承認。品質スコア下降時は自動ロールバック

**使えば使うほど賢くなる蓄積型の堀（Moat）。** 競合が真似しても、蓄積された審美データとCritic重みは奪えない。

---

## 9. 品質ティア

| ティア | パイプライン | マルチモデル | Enhancer | Taste Engine | リトライ上限 | コスト/件 | 用途 |
|-------|------------|-----------|---------|-------------|-----------|---------|------|
| **Preview** | 最速モデル1つ | ❌ | ❌ | ❌ | 0回 | ~$0.01 | ラフ・プレビュー |
| **Standard** | 最適モデル1つ | ❌ | ✅ | ✅ | 3回 | ~$0.10 | 一般外販 |
| **Premium** | マルチモデル合成 | ✅ | ✅ | ✅ | 5回 | ~$0.50 | ハイエンド案件 |
| **Masterpiece** | Premium + AI Creative Director + Mad Editor AI | ✅ | ✅ | ✅(6Critic) | 10回 | $2.00+ | フラッグシップIP |

**最低ライン（Standard）が競合の最高ラインを超えている**状態を維持する。

---

## 10. CreativeBlueprint（統一設計図）

### B2B例

```json
{
  "blueprint_id": "enterprise-brand-kit-v1",
  "business_model": "b2b",
  "entity_config": {"type": "company", "industry": "saas", "brand_maturity": "established"},
  "quality_tier": "premium",
  "deliverables": ["logo_variations", "color_palette", "lp_hero", "social_banners"],
  "reference_set_id": "ref-set-enterprise-saas-2026",
  "taste_config": {"brand_weight": 0.4, "impact_weight": 0.3, "composition_weight": 0.2, "color_weight": 0.1}
}
```

### B2C例

```json
{
  "blueprint_id": "indie-mv-factory-v1",
  "business_model": "b2c",
  "entity_config": {"type": "consumer", "creator_level": "indie_artist"},
  "quality_tier": "standard",
  "deliverables": ["music_video_60s", "thumbnail", "social_clips"],
  "style_pack": "user_selected",
  "model_preference": "auto"
}
```

### C2C例（スタイルパック）

```json
{
  "blueprint_id": "style-pack-marketplace",
  "business_model": "c2c",
  "entity_config": {"supply": {"type": "creator"}, "demand": {"type": "consumer"}},
  "style_pack_config": {
    "creation": {"prompt_template": true, "parameter_set": true, "reference_images": 5},
    "marketplace": {"royalty_rate": 0.30, "pricing": "creator_set"},
    "quality_gate": {"min_taste_score": 80}
  }
}
```

---

## 11. Boundary Protocol

```
Lumina = クリエイティブ素材の制作（生成・品質保証・納品）
Ada    = LLM推論基盤の提供（Luminaは呼び出しのみ）
Cyrus  = Luminaへの素材発注（Cyrusは発注側）
Noah   = C2Cマーケットプレイスの場（LuminaはスタイルパックのY）
```

- Lumina → Adaの方向はAPI経由の**推論リクエスト**のみ
- Lumina → Noahの方向はAPI経由の**スタイルパックデータ納品**のみ
- **Luminaはクリエイティブ制作のドメインから一歩も出ない**

---

## 12. 技術スタック

| 領域 | 技術 |
|------|------|
| グラフエンジン | LangGraph |
| LLMプロバイダー | Ada Core API |
| 画像生成 | Model Registry管理（Flux / DALL-E / SD XL / Midjourney 等、動的選択） |
| 映像生成 | Model Registry管理（Sora / Runway / Pika 等、動的選択） |
| ベクトルDB | Supabase pgvector（Reference Set） |
| API | FastAPI |
| 状態管理 | LangGraph AsyncPostgresSaver |
| キュー | Redis Streams → AWS SQS |
| アセットStorage | Supabase Storage / S3 |
| 決済 | Stripe |
| テスト | pytest + pytest-asyncio |
| 並列実行 | LangGraph Send() API |

---

## 13. 外販プロダクト

| Tier | 価格 | 内容 |
|------|------|------|
| **Free** | ¥0 | 月5画像（透かし付き）/ 30秒MV 1本/月 |
| **Lumina Studio** | ¥5-100万/案件 | ブランドキット・LP・広告クリエイティブ・MV |
| **MV Factory** | ¥5-30万/本 | 楽曲解析→ビート同期MV自動生成 |
| **Asset Sub** | ¥1-5万/月 | 商用AI生成素材のサブスク |
| **Style Pack Marketplace** | Creatorが価格設定 + Lumina手数料30% | C2Cスタイルパック売買 |
| **Enterprise** | カスタム | 専属Taste Engine校正 + ブランドReference Set構築 |

---

## 14. 戦闘力ロードマップ

| 時点 | 戦闘力 | マイルストーン |
|------|--------|-------------|
| **M2 W1: Creation MVP** | 10/100 | brief_interpreter + model_selector + generator。単一モデル画像生成 |
| **M2 W2: +Quality Fortress** | 30/100 | taste_engine + quality_score_cascade。品質保証開始 |
| **M2 W3: +Enhancer + MV** | 50/100 | enhancer_pipeline + 映像生成パイプライン。**Standard品質確立** |
| **M2 W4: +Delivery + PLG** | 60/100 | format_optimizer + asset_packager。Free Tier公開 |
| **M3: +Multi-Model + Premium** | 75/100 | multi_model_compositor。Premium品質確立 |
| **M3: +Evolution** | 85/100 | sota_watchdog + taste_calibrator + predictive_qc。**自律品質更新開始** |
| **M4: +C2C Style Pack** | 90/100 | Noah連携。スタイルパックマーケットプレイス |
| **M6: +L3 Full Auto** | 95/100 | 完全自律。モデル切り替え・スタイル更新・品質校正が全て自動 |

---

## 15. KPI

### 外販KPI

| 指標 | 目標 |
|------|------|
| MRR | ¥50万（M4時点） |
| PLG無料ユーザー | 500名 |
| Free → Paid CVR | 5%+ |
| Taste Engine平均スコア | 85/100+ |

### 内販KPI

| 指標 | 目標 |
|------|------|
| 月間アセット供給件数 | 100件+ |
| Velie QA Gate通過率 | 98%+ |
| Taste Engine通過率（Standard以上） | 90%+ |
| 平均イテレーション回数 | 1.5回以下 |
| MV自動生成時間 | <10分/曲 |

---

> **最終更新**: 2026-02-22（v2.1 — Round 8: 完全AIカンパニー化。人間依存パス4件を全てAI化）
> **ディベート**: holdings_blueprint.md 全19ラウンド + 品質×B2B/B2C/C2Cディベート8ラウンド完走（CEO承認済み）
> **設計図完成度**: 95/100
> **次のアクション**: M2 W1 — Creation Layer MVP（brief_interpreter + model_selector + generator）の実装
